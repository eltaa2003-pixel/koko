export const PREFIX = '.';

// Where session credentials get written. Treat this folder like a password —
// don't commit it, don't share it.
export const AUTH_FOLDER = 'auth_info';

// Default cooldown (seconds) applied to any plugin that doesn't set its own.
export const DEFAULT_COOLDOWN = 2;
